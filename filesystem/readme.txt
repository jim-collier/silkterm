This is an example filesystem structure. Optionally, you can copy/paste it to your own filesystem to create default files.

home/ mirrors a user home directory ($HOME) - e.g. home/.config/silkterm/backgrounds/ holds the optional background image pack. (The directory was previously named literally "~", which risked shell mishaps.)
